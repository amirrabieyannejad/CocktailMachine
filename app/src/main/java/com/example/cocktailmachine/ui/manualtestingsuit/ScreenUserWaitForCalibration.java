package com.example.cocktailmachine.ui.manualtestingsuit;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class ScreenUserWaitForCalibration extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        //setContentView(R.layout.activity_scean_user_wait_for_calibration);



        //LinearLayout myLayout = findViewById(R.layout.activity_scean_user_wait_for_calibration);

        /**Button myButton = new Button(this);
        myButton.setLayoutParams(new LinearLayout.LayoutParams(
                LinearLayout.LayoutParams.MATCH_PARENT,
                LinearLayout.LayoutParams.MATCH_PARENT));

        myLayout.addView(myButton);
        findViewById(R.layout.activity_scean_user_wait_for_calibration)*/
    }
}